name: <name>
channels:
  - conda-forge
  - defaults
dependencies:
  - isort
  - mypy
  - pip
  - pylint
  - pytest
  - pip:
    - black
    - coverage
    - flake8
    - flake8-bandit
    - flake8-bugbear
    - flake8-comprehensions
    - pre-commit
    - pytest-console-scripts
    - pytest-cov
    - pytest-env
    - pytest-mock
